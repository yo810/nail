<div class="container">
	<h2 class="title text-center pb-3">Photo</h2>
	<div class="row title">

		<table class="table table-hover table-bordered">
			<thead>
				<tr>
					<th>Photo ID</th>
					<th>Photo 名</th>
				</tr>
			</thead>
			<tbody>
				<% @photos.each do |photo| %>
					<tr>
						<td><%= photo.id %></td>
						<td><%= link_to photo.name, admin_photo_path(photo) %></td>
					</tr>
				<% end %>
			</tbody>
		</table>

		<%= paginate @photos %>

  </div>
</div>