<div class="container">
  <div class="row">
    <div class="col-12 text-center title">
      <h2 class="pb-5">予約完了画面</h2>
      <h4>ご予約ありがとうございます。</h4>
    </div>
    <div class="col-12 mt-3 text-center content title">
      <h5>予約内容</h5>
      <table class="col-12 table table-hover table-bordered">
        <tr>
          <td>メニュー</td>
          <td><%= @menu.name %> <%= @menu.course %></td>
        </tr>
        <tr>
          <td>来店日時</td>
          <td><%= @reservation.day %> <%= @reservation.time %></td>
        </tr>
      </table>
    </div>
    <div class="col-12 text-right">
      <%= link_to '予約画面に戻る', new_menu_reservation_path(@menu, day: @reservation.day, time: @reservation.time) %>
    </div>
  </div>
</div>